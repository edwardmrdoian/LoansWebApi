namespace Loans.Domain
{
    public enum Role { User = 0, Accountant = 1 }
    public enum LoanType { FastLoan = 0, AutoLoan = 1, Installment = 2 }
    public enum LoanStatus { InProcess = 0, Approved = 1, Rejected = 2 }

}
